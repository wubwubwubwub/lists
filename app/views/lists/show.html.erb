This is the LIST SHOW PAGE
<p>
  <strong>List Name: </strong><%= @list.list_name.capitalize %>
</p>
<p>
  <strong>Notes: </strong><%= @list.notes.capitalize %>
</p>

<ol>
  <% @list.items.each do |item| %>
    <li><%= item.name.capitalize %><%= " - #{item.quan.capitalize}" unless item.quan.blank? %><%= " - #{item.notes.capitalize}" unless item.notes.blank? %></li>
<% end %>
</ol>

<h4><%= link_to 'Edit this list', edit_list_path %>